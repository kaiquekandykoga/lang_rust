# lang_rust

## nerdctl
```
nerdctl build -t ci-basic-github-rust .
nerdctl run --rm ci-basic-github-rust
```

## docker
```
docker build -t ci-basic-github-rust .
docker run --rm ci-basic-github-rust
```
